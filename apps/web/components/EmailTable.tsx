export function EmailTable({ rows }: { rows:Array<{id:string;recipientEmail:string;subject:string;status:string;scheduledAt:string;sentAt?:string;lastError?:string}> }) { return <div className="table-wrap"><table><thead><tr><th>Recipient</th><th>Subject</th><th>Status</th><th>When</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td>{row.recipientEmail}</td><td>{row.subject}</td><td><span className={`status ${row.status.toLowerCase()}`}>{row.status}</span>{row.lastError&&<small>{row.lastError}</small>}</td><td>{new Date(row.sentAt??row.scheduledAt).toLocaleString()}</td></tr>)}</tbody></table></div>; }
